import CommonTitle from "@/common/Title";

const Notification = () => {
  return (
    <>
      <CommonTitle
        title="Notifications"
        subTitle="Recent updates and approvals."
      />
    </>
  );
};

export default Notification;
